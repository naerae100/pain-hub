/**
 * Feature flags for showing/hiding content sections.
 * Set to `false` to hide, `true` to show.
 */
export const FEATURES = {
    LASER_SYSTEMS: false, // Toggle laser systems & PLDD content site-wide
};
